[project]
name = "myrepo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JUN-HYEOK SEO", email = "starlights2283@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
myrepo = "myrepo:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
